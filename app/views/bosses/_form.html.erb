<%= form_with(model: boss, local: true, html: {class: "form-horizontal"}) do |form| %>
  <% if boss.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(boss.errors.count, "error") %> prohibited this boss from being saved:</h2>

      <ul>
      <% boss.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :wing %>
    <%= form.text_field :wing, id: :boss_wing, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :name %>
    <%= form.text_field :name, id: :boss_name, class: 'form-control' %>
  </div>

  <div class="form-group">
	  <%= form.submit class: 'btn btn-primary' %>
	  <%= link_to 'Back', bosses_path, class: 'btn btn-secondary' %>
  </div>
<% end %>
